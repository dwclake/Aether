(test
  (name aether)
  (modules aether)
  (libraries lib alcotest
             lexer_t
             parser_t))
(library
  (name lexer_t)
  (modules lexer_t)
  (libraries lib alcotest))

(library
  (name parser_t)
  (modules parser_t)
  (libraries lib alcotest))
